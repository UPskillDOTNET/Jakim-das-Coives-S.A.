@startuml
skinparam classAttributeIconSize 0

class UsersController {
  +Post Login()
  +Post Register()
  +Delete DeleteUser(nif)
  +UserToken BuildToken(user)
}

class User {
  -Int nif PK
  -String companyName
  -String email
  -String password

  +User(nif,companyName,email,password)
}

class Context {
  -DbSet<User> Users
  -DbSet<Reserva> Reservas
  -DbSet<Lugar> Lugares
}

class UserToken{
  -String token
  -DateTime expiration
}

UsersController ..> User
UsersController ..> Context
UsersController ..> UserToken

Context "1" --> "0..*" User: tem
@enduml