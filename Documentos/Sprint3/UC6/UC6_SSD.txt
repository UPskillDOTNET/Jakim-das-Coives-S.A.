@startuml
autonumber
actor "Utilizador" as User
actor ":API.SubAluguer" as API

activate User
User -> API : inicia pedido de sub-aluguer
activate API
API --> User : solicita dados lugar pretendido \n(id e data/hora)
deactivate API

User -> API : introduz os dados solicitados
activate API

API --> User : operação bem sucedida

deactivate API

deactivate User
@enduml